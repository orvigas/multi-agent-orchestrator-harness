node_modules/
dist/
.env
.harness.local/

# Artefactos runtime del propio harness (Capa 8, Merge Manager, Capa 1) —
# generados por npm run dev / harness:execute / merge-manager:demo, no son
# código fuente.
.harness/releases.jsonl
.harness/escalations/
.harness/runs.jsonl

# Settings/estado locales de Claude Code (por-máquina/sesión, no compartidos).
.claude/settings.local.json
.claude/scheduled_tasks.lock

# Cruft del sistema operativo / build.
.DS_Store
*.tsbuildinfo
